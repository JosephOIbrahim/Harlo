data/*
# Exception: the synthetic trajectories used to train the cognitive
# predictor are tracked via git-lfs (see .gitattributes). The rest of
# data/ (twin.db, observations.db, stages/, …) stays user-local.
# `data/*` is used instead of `data/` so the negation below takes
# effect — git can't re-include files inside an ignored directory.
!data/trajectories_10k.jsonl
target/
target312/
__pycache__/
*.pyc
.DS_Store
.venv/
.venv312/
*.egg-info/
build/
dist/
*.so
*.dylib
*.pyd
*.pdb
models/*
# Exception: the trained cognitive predictor is tracked via git-lfs
# (see .gitattributes). Other model artifacts (bge-small, etc.) stay
# user-local. Same `models/*` pattern reason as above.
!models/cognitive_predictor_v1.joblib
encoder_output/
.hypothesis/

# Secrets
.env
.env.*
*.key
*.pem
*.secret

# Local config
.mcp.json

# Patent confidential (safety net)
docs/patent/
specs/
files/

# Build artifacts
.agent-team/
.coverage
.pytest_cache/
.claude/

# Phase 5B — xcodegen output (regenerated each build)
macos/HarloHealthBridge/HarloHealthBridge.xcodeproj/
macos/HarloHealthBridge/.build/

# session scratch / capsules
.fieldhouse/
CLAUDE-FABLE-5.md
