# cold cell — no Harlo at all.
# The daemon stays DOWN and the MCP server stays unmounted for this cell;
# run_cell.sh enforces the former and reminds the operator of the latter.
# No environment variables on purpose.
