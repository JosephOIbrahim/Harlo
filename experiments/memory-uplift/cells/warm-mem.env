# warm-mem cell — memory available, engine telemetry OFF.
# Values must be the literal strings "1"/"0": engine_config.py parses
# with == "1" (python/harlo/engine/engine_config.py:24-33).
ENGINE_ENABLED=1
USE_REAL_USD=1
OBSERVATION_LOGGING=0
PREDICTION_ENABLED=0
