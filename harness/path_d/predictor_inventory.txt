=== PVH Phase 0 (re-run) — Predictor Load Contract (Forge task 3) ===
Tree: post-merge defab04. Predictor materialized via git-lfs (D26).
Command (run in .venv312):
  from joblib import load
  m = load('models/cognitive_predictor_v1.joblib')
  -> introspect type, outputs, feature count, feature names

RESULT: PASS — predictor loads and introspects.

model type:            MultiOutputRegressor
n_estimators (outputs): 4              # XGBRegressor per target
inner estimator type:  XGBRegressor
feature_count:         111             # est0.n_features_in_
feature names:         no feature_names_in_  (trained on an unnamed numpy matrix)

=== Feature-set confirmation (resolves RSI item 2 / D21) ===
feature_count = 111 confirms the static reconstruction from
src/train_predictor.py:
  per observation = 7 (state) + 10 (ActionType one-hot) + 9 (dynamics)
                  + 5 (InjectionProfile one-hot) + 1 (alpha) + 1 (phase)
                  + 4 (allostasis) = 37
  window = 37 x 3 observations = 111
The delegate block is NOT encoded; there is NO delegate_id feature.
feature_names_in_ is absent (XGBoost on an unnamed array), so the names
cannot be enumerated dynamically — the static derivation above is the
authoritative feature map. Gate 0 criterion 3 satisfied via the explicit
"no feature_names_in_" branch, with feature_count captured.

=== Artifact provenance ===
- File: models/cognitive_predictor_v1.joblib (377K; LFS oid sha256:dde0dfd8..,
  size 385803).
- Materialized by `git lfs pull` after merging origin/master (the LFS tracking
  in .gitattributes + the !models/...joblib un-ignore arrived with the merge).
- PVH did not train it; per D26 the pull is repo-state prep, not a PVH write.

=== Prior (pre-fix) result, for the record ===
On the original Phase 0 checkout (0498ee1) this step FAILED with
FileNotFoundError — the LFS-tracked predictor was absent because the local
branch was 13 commits behind origin/master. See 05_DECISIONS.md D22/D26 and
the Step 2 observation. The FAIL artifact is preserved in commit 1cf4174.
